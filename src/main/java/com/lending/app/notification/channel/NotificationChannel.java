package com.lending.app.notification.channel;

/**
 * Enum representing the different notification channels available for sending
 * notifications.
 */
public enum NotificationChannel {
    EMAIL,
    SMS,
    PUSH
}
